retext:
  max_iterations: 20
  discrimination_threshold: 0.7
  discrim_api_url: "https://labelguard.lab.sspcloud.fr"
  qdrant_collection: "original_cleaned"
  session_id: "fast-test"

prompt:
  prompt_path: "system/retext/"

main:
  n_labels_per_gen: 5